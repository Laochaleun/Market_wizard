# Market Wizard Backend

## Environment Variables
GOOGLE_API_KEY=your-google-api-key
OPENAI_API_KEY=your-openai-api-key  # Optional, for text-embedding-3-small

## LLM Settings
LLM_MODEL=gemini-3-flash-preview
RESEARCH_LLM_MODEL=gemini-2.5-flash-lite
RESEARCH_INTERPRETATION_MODEL=gemini-3-flash-preview
# Optional research fallbacks
# RESEARCH_PLAYWRIGHT_FALLBACK_LIMIT=2
# RESEARCH_PLAYWRIGHT_TIMEOUT_MS=15000
LLM_TEMPERATURE=0.5
REPORT_ANALYSIS_MODEL=gemini-3-pro-preview
REPORT_ANALYSIS_THINKING_BUDGET=256

## Embedding Settings
EMBEDDING_PROVIDER=local  # "local" (BGE-M3) or "openai" (text-embedding-3-small)
EMBEDDING_MODEL=BAAI/bge-m3
SSR_TEMPERATURE=1.0
SSR_EPSILON=0.0
SSR_CALIBRATION_ENABLED=true
SSR_CALIBRATION_ARTIFACT_PATH=backend/app/data/ssr_calibrator_default.json
SSR_CALIBRATION_POLICY_PATH=backend/app/data/ssr_calibration_policy_default.json

## Database
DATABASE_URL=sqlite+aiosqlite:///./market_wizard.db

## GUS API (optional, for demographic data)
GUS_API_KEY=  # Leave empty for anonymous access (with rate limits)
